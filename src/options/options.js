// ... existing code ...

// Add new setting for image blur
const settings = {
  // ... existing settings ...
  enableImageBlur: {
    default: true,
    type: 'boolean'
  }
};

// ... existing code ...