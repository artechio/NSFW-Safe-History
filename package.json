{
  "name": "nsfw-safe-history",
  "version": "2.0.7",
  "description": "Chrome extension: clear adult sites from history on visit and blur NSFW images on-device",
  "main": "src/background.js",
  "scripts": {
    "build": "webpack --mode production && npm run build --prefix ext-ui",
    "build:core": "webpack --mode production",
    "build:ui": "npm run build --prefix ext-ui",
    "dev": "webpack --mode development --watch",
    "dev:ui": "npm run dev --prefix ext-ui",
    "test": "node --test",
    "update-blocklist": "node scripts/update-blocklist.js",
    "download-model": "node scripts/download-model.js",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/nsfw-safe-history"
  },
  "keywords": [
    "chrome-extension",
    "privacy",
    "nsfw",
    "history"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "nsfwjs": "^2.4.2"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "sharp": "^0.33.5",
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4"
  }
}
